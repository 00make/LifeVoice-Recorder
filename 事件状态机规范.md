# 事件状态机规范（LifeOS）

## 1. 状态定义

- `received`：事件已接收
- `queued`：入队等待
- `processing`：处理中
- `done`：完成
- `failed`：失败

## 2. 状态迁移规则

- `received` → `queued`
- `queued` → `processing`
- `processing` → `done`
- `processing` → `failed`

## 3. 失败处理

- `failed` 可重试 3 次
- 超过次数转入死信队列
